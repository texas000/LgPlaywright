name: LG Production Synthetic Monitor

on:
  # schedule:
  #  - cron: "*/30 * * * *"
  workflow_dispatch: {}  # 수동 실행 가능
  push:
    branches:
      - main
    paths:
      - 'tests/**'
      - '.github/workflows/**'

permissions:
  contents: read
  actions: write

jobs:
  obs-monitor:
    name: OBS Store Tests
    runs-on: ubuntu-latest
    timeout-minutes: 15

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 18
          cache: 'npm'

      - name: Cache node modules
        uses: actions/cache@v4
        id: npm-cache
        with:
          path: node_modules
          key: ${{ runner.os }}-node-${{ hashFiles('**/package-lock.json') }}
          restore-keys: |
            ${{ runner.os }}-node-

      - name: Cache Playwright browsers
        uses: actions/cache@v4
        id: playwright-cache
        with:
          path: ~/.cache/ms-playwright
          key: ${{ runner.os }}-playwright-${{ hashFiles('**/package-lock.json') }}
          restore-keys: |
            ${{ runner.os }}-playwright-

      - name: Install dependencies
        if: steps.npm-cache.outputs.cache-hit != 'true'
        run: npm ci

      - name: Install Playwright browsers
        if: steps.playwright-cache.outputs.cache-hit != 'true'
        run: npx playwright install --with-deps

      - name: Run OBS Add to Cart Test
        run: npx playwright test tests/lg-add-to-cart.spec.ts --timeout=120000
        continue-on-error: true
        env:
          CI: true

      - name: Display Test Summary
        if: always()
        run: |
          echo "=== OBS Test Execution Summary ==="
          if [ -f "test-output.log" ]; then
            cat test-output.log
          fi
          echo "=== End of Summary ==="

      - name: Upload HTML Report
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: obs-html-report-${{ github.run_number }}
          path: playwright-report/
          retention-days: 30

      - name: Upload Test Artifacts
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: obs-test-artifacts-${{ github.run_number }}
          path: |
            test-results/
            **/*.png
            **/*.webm
            **/*trace.zip
          retention-days: 30

      - name: Send Slack notification on failure
        if: failure()
        uses: rtCamp/action-slack-notify@v2
        continue-on-error: true
        env:
          SLACK_WEBHOOK: ${{ secrets.SLACK_WEBHOOK }}
          SLACK_COLOR: "#ff0000"
          SLACK_TITLE: "❌ OBS Add to Cart Test Failed"
          SLACK_MESSAGE: |
            OBS 테스트 실행 실패!
            Run: ${{ github.run_number }}
            Workflow: ${{ github.workflow }}
            Branch: ${{ github.ref_name }}
            
            📊 리포트: ${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}
          SLACK_FOOTER: "LG OBS Monitor"

      - name: Send Slack notification on success
        if: success()
        uses: rtCamp/action-slack-notify@v2
        continue-on-error: true
        env:
          SLACK_WEBHOOK: ${{ secrets.SLACK_WEBHOOK }}
          SLACK_COLOR: "#36a64f"
          SLACK_TITLE: "✅ OBS Add to Cart Test Passed"
          SLACK_MESSAGE: |
            OBS 테스트 실행 성공!
            Run: ${{ github.run_number }}
            
            ✅ Add to Cart 테스트 성공
          SLACK_FOOTER: "LG OBS Monitor"

  pm-monitor:
    name: PM Store Tests
    runs-on: ubuntu-latest
    timeout-minutes: 15

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 18
          cache: 'npm'

      - name: Cache node modules
        uses: actions/cache@v4
        id: npm-cache
        with:
          path: node_modules
          key: ${{ runner.os }}-node-${{ hashFiles('**/package-lock.json') }}
          restore-keys: |
            ${{ runner.os }}-node-

      - name: Cache Playwright browsers
        uses: actions/cache@v4
        id: playwright-cache
        with:
          path: ~/.cache/ms-playwright
          key: ${{ runner.os }}-playwright-${{ hashFiles('**/package-lock.json') }}
          restore-keys: |
            ${{ runner.os }}-playwright-

      - name: Install dependencies
        if: steps.npm-cache.outputs.cache-hit != 'true'
        run: npm ci

      - name: Install Playwright browsers
        if: steps.playwright-cache.outputs.cache-hit != 'true'
        run: npx playwright install --with-deps

      - name: Run PM Member Login Test
        run: npx playwright test tests/lg-member-login.spec.ts --timeout=180000
        continue-on-error: true
        env:
          CI: true
          PM_MEMBER_EMAIL: ${{ secrets.PM_MEMBER_EMAIL }}
          PM_MEMBER_PASSWORD: ${{ secrets.PM_MEMBER_PASSWORD }}

      - name: Display Test Summary
        if: always()
        run: |
          echo "=== PM Test Execution Summary ==="
          if [ -f "test-output.log" ]; then
            cat test-output.log
          fi
          echo "=== End of Summary ==="

      - name: Upload HTML Report
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: pm-html-report-${{ github.run_number }}
          path: playwright-report/
          retention-days: 30

      - name: Upload Test Artifacts
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: pm-test-artifacts-${{ github.run_number }}
          path: |
            test-results/
            **/*.png
            **/*.webm
            **/*trace.zip
          retention-days: 30

      - name: Send Slack notification on failure
        if: failure()
        uses: rtCamp/action-slack-notify@v2
        continue-on-error: true
        env:
          SLACK_WEBHOOK: ${{ secrets.SLACK_WEBHOOK }}
          SLACK_COLOR: "#ff0000"
          SLACK_TITLE: "❌ PM Member Login Test Failed"
          SLACK_MESSAGE: |
            PM 테스트 실행 실패!
            Run: ${{ github.run_number }}
            Workflow: ${{ github.workflow }}
            Branch: ${{ github.ref_name }}
            
            📊 리포트: ${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}
          SLACK_FOOTER: "LG PM Monitor"

      - name: Send Slack notification on success
        if: success()
        uses: rtCamp/action-slack-notify@v2
        continue-on-error: true
        env:
          SLACK_WEBHOOK: ${{ secrets.SLACK_WEBHOOK }}
          SLACK_COLOR: "#36a64f"
          SLACK_TITLE: "✅ PM Member Login Test Passed"
          SLACK_MESSAGE: |
            PM 테스트 실행 성공!
            Run: ${{ github.run_number }}
            
            ✅ Member Login 테스트 성공
          SLACK_FOOTER: "LG PM Monitor"
